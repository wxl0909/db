<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.decryto.mapper.UserMapper">

<!--<sql id="">

</sql>-->
    <select id="findUser" resultType="user">
        select user_name title, ID,user_name name,user_password password from user where user_name=#{name} and user_password=#{password};
    </select>

<!--增删改-->
    <insert id="saveUser">
        insert into user (user_name,user_password,
        create_time,update_time,user_role_id) values (#{name},#{password},
        #{dateTime},#{updateTime},#{userRoleId})
    </insert>

    <!--修改密码-->
    <update id="updateUser">
        update user set password=#{password} where id = #{id}
    </update>
    <delete id="delUser">
        delete from user where id=#{id}
    </delete>

    <!--查询用户-->
    <select id="findUserList" resultType="user">
        select u.user_name name,u.user_role_id userRoleId,u.id,u.user_status status,r.role_name roleName
from user u LEFT JOIN role r on u.user_role_id=r.id
    </select>
    <!--更新信息-->
    <update id="uptUser">
        update user set user_name=#{name} where id = #{id}
    </update>
    <!--删除对应的权限节点-->
    <delete id="delPUser">
        delete from role_permit where role_id=#{userRoleId}
    </delete>
</mapper>